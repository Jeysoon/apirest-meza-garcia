/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { DefaultFontHeaderDirective } from './defaultFontHeader.directive';

describe('Directive: DefaultFontHeader', () => {
  it('should create an instance', () => {
    const directive = new DefaultFontHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
